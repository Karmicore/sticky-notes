# V0.3.0 需求讨论

> 两个大方向：**插件化架构重构** + **AI 功能接入**。
> 架构先行，AI 功能作为插件接入。

---

## 一、插件化架构

### 目标

新增功能 = 新增一个目录，不改或极少改已有文件。

### 现状问题

| 问题 | 具体表现 |
|------|----------|
| `lib.rs` 是单体入口 | 所有命令注册、快捷键、托盘、窗口初始化都在一个 `run()` 里 |
| Note 是单体结构体 | 导出加字段、折叠加字段 → 每次改 note.rs + sqlite_storage |
| `main.jsx` 硬编码路由 | 加新窗口类型要改路由逻辑 |
| 没有事件总线 | 插件间通信靠直接函数调用 |
| 前后端不对称 | 前端已有 registry 模式，后端没有 |

### 后端设计 (Rust)

**Plugin Trait:**
```rust
pub trait Plugin: Send + Sync {
    fn id(&self) -> &str;
    fn name(&self) -> &str;
    fn commands(&self) -> Vec<CommandDef>;
    fn setup(&self, app: &AppHandle) -> Result<(), String> { Ok(()) }
    fn tray_menu_items(&self) -> Vec<TrayMenuItem> { vec![] }
    fn migrations(&self) -> Vec<String> { vec![] }
}
```

**目录结构:**
```
src-tauri/src/
  app_core/            ← 不变
  infra/               ← 不变
  plugin_system/       ← 新增
    plugin.rs          ← Plugin trait
    registry.rs        ← PluginRegistry
    event_bus.rs       ← 事件总线
  plugins/             ← 从 commands/ 迁移
    core/              ← 核心插件
    export/            ← 导出插件
    config/            ← 配置插件
  lib.rs               ← 只做聚合注册
```

**lib.rs 变为:**
```rust
.invoke_handler(tauri::generate_handler![
    // ── core plugin ──
    plugins::core::get_note,
    plugins::core::save_note,
    // ...
    // ── export plugin ──
    plugins::export::export_notes_copy,
    // ...
])
```

### 前端设计 (React)

**插件注册:**
```js
// src/plugins/core/index.js
export const corePlugin = {
  id: "core",
  commands: { "note.new": { ... }, ... },
  menuStructure: [ ... ],
  keyMap: { ... },
  components: {},
};
```

**main.jsx 变为:**
```jsx
registerPlugin(corePlugin);
registerPlugin(exportPlugin);

function App() {
  const components = getAllComponents();
  const label = getCurrentWindow().label;
  for (const [prefix, Component] of Object.entries(components)) {
    if (label.startsWith(prefix)) return <Component />;
  }
}
```

**目录结构:**
```
src/
  plugin-system/       ← 新增
    mod.js
    plugin-registry.js
    event-bus.js
  plugins/             ← 从 features/ + commands/ 迁移
    core/
      index.js
      NoteWindow.jsx
      NoteEditor.jsx
      TitleBar.jsx
      hooks/
      utils/
      styles/
    export/
      index.js
      ExportPopup.jsx
    config/
      index.js
  lib/                 ← 不变
  main.jsx             ← 简化为插件路由
```

### 数据扩展

Note 加 `metadata` JSON 字段，插件存私有数据：

```rust
// note.rs
#[serde(default)]
pub metadata: serde_json::Value,  // {"remind": {"at": "..."}, "tags": ["work"]}
```

```sql
ALTER TABLE notes ADD COLUMN metadata TEXT NOT NULL DEFAULT '{}';
```

### 迁移计划

| Phase | 内容 | 风险 |
|-------|------|------|
| Phase 1 | 建立插件骨架，不改功能 | 低 |
| Phase 2 | 迁移现有功能到插件 | 中 |
| Phase 3 | 事件总线 + metadata | 低 |
| Phase 4 | 用新功能验证插件架构 | 无 |

---

## 二、AI 功能

### 整体架构

```
┌─────────────────────────────────────────────┐
│  AI 功能插件                                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ ai-share │ │ ai-report│ │ ai-coach │    │
│  │ 润色分享  │ │ 日报/周报 │ │ AI教练   │    │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘    │
│       └────────────┼────────────┘           │
│                    ▼                        │
│            ┌───────────────┐                │
│            │  AI Service   │  统一调用层     │
│            └───────┬───────┘                │
│                    ▼                        │
│        Claude API  │  OpenAI API            │
└─────────────────────────────────────────────┘
```

### Provider Trait

同时支持 Claude 和 OpenAI：

```rust
#[async_trait]
pub trait AiProvider: Send + Sync {
    fn name(&self) -> &str;
    fn models(&self) -> Vec<ModelInfo>;
    async fn complete(&self, model: &str, req: &AiRequest) -> Result<AiResponse, AiError>;
    async fn complete_stream(&self, model: &str, req: &AiRequest)
        -> Result<Receiver<AiChunk>, AiError>;
}
```

- Claude: POST `https://api.anthropic.com/v1/messages`
- OpenAI: POST `https://api.openai.com/v1/chat/completions`
- 两者都支持流式响应

### API Key 管理

用户自备 key，存本地 config.json，Rust 端发请求，前端永远不碰 key。

```json
{
  "ai": {
    "claude_api_key": "sk-ant-...",
    "openai_api_key": "sk-...",
    "openai_base_url": "https://api.openai.com",
    "default_provider": "claude",
    "default_model": "claude-sonnet-4-20250514"
  }
}
```

### 流式响应

通过 Tauri Event 推送 chunks 到前端：

```rust
while let Some(chunk) = stream.recv().await {
    app.emit("ai-stream-chunk", AiChunkEvent {
        request_id,
        delta: chunk.delta,
        done: chunk.done,
    }).ok();
}
```

前端监听事件，逐字显示。

### 新增依赖

```toml
# Cargo.toml
reqwest = { version = "0.12", features = ["json", "stream"] }
tokio = { version = "1", features = ["full"] }
```

---

## 三、AI 润色分享

### 场景

便签内容太口语化，想分享到朋友圈/推特/邮件，需要润色。

### 入口

便签右键菜单 → AI 润色 → 选择风格（朋友圈/推特/邮件/通用）

### 交互流程

```
右键便签 → AI 润色 → 朋友圈风格
  ↓
弹出润色窗口:
┌─────────────────────────────────┐
│  ✨ AI 润色 — 朋友圈风格         │
├─────────────────────────────────┤
│  原文:                          │
│  ┌─────────────────────────┐    │
│  │ 方案写完了，跟老板review  │    │
│  └─────────────────────────┘    │
│  润色结果:（流式显示）            │
│  ┌─────────────────────────┐    │
│  │ 📝 Q2 产品方案定稿完成！ │    │
│  │ 下一步跟老板 review...   │    │
│  └─────────────────────────┘    │
│  [复制结果] [换一个风格] [关闭]   │
└─────────────────────────────────┘
```

### Prompt

| 风格 | System Prompt 要点 |
|------|-------------------|
| 朋友圈 | 保持核心意思，流畅自然，可加 emoji，200 字以内 |
| 推特 | 简洁有力，可加 emoji，280 字符以内 |
| 邮件 | 结构清晰，语气专业 |
| 通用 | 更清晰有条理，保持原意 |

### 技术

- 单个便签作为输入，不需要时间戳
- temperature 0.8（需要创意）
- max_tokens 1024
- 不需要用户画像

---

## 四、AI 日报 / 周报

### 场景

- 下班前: "今天干了什么？" → 生成日报
- 周五: "这周做了什么？" → 生成周报

### 入口

托盘菜单 → AI → 生成日报 / 生成周报

### 日报格式

```markdown
## 今日完成
- [x] 完成项目方案初稿
- [x] 回复客户邮件

## 进行中
- [ ] Q2 产品规划（完成 3/5）

## 明日建议
- 优先完成 Q2 规划剩余部分

## 一句话
今天完成了 3 个待办，节奏不错。
```

### 周报额外内容

- 本周概览（便签总数、待办完成率、最活跃日）
- 待办拖延分析（哪些待办拖了超过 3 天）
- 趋势对比

### 技术

- 按 `updated_at` 过滤今日/本周便签
- 解析便签内容中的 ☐☑☒ 统计待办
- temperature 0.5（要准确）
- max_tokens 2048
- 注入用户画像，明日建议围绕目标

### 前置依赖

Note 需要加 `created_at` / `updated_at` 字段：

```sql
ALTER TABLE notes ADD COLUMN created_at TEXT NOT NULL DEFAULT '';
ALTER TABLE notes ADD COLUMN updated_at TEXT NOT NULL DEFAULT '';
UPDATE notes SET created_at = datetime('now'), updated_at = datetime('now')
  WHERE created_at = '';
```

---

## 五、AI 教练

### 核心价值

帮用户从自己的便签数据里发现他没意识到的模式。

### 入口

- 托盘菜单 → AI → 教练
- 快捷键: Alt+C
- 便签右键 → 问 AI 教练（带上下文）

### 分析维度

| 维度 | 分析什么 | 依赖 |
|------|----------|------|
| 时间管理 | 便签创建时间分布、高效/低效时段 | created_at |
| 完成率 | 待办完成比例、拖延模式 | ☐☑☒ 解析 |
| 内容质量 | 便签是否具体、有无行动项 | title + content |
| 习惯模式 | 颜色使用、折叠/锁定行为 | color, collapsed, locked |

### 交互流程

```
打开教练窗口:
┌─────────────────────────────────────┐
│  🧠 AI 教练                          │
├─────────────────────────────────────┤
│  你好！选择一个分析维度:              │
│  [📊 时间管理] [✅ 完成率]            │
│  [📝 内容质量] [🔍 习惯模式]          │
│  或者直接问我任何问题...               │
│  ┌─────────────────────────┐ [发送]  │
│  └─────────────────────────┘         │
└─────────────────────────────────────┘

点击 "完成率":
┌─────────────────────────────────────┐
│  📊 完成率分析（最近 7 天）            │
├─────────────────────────────────────┤
│  待办完成: 12/18（67%）              │
│  技术任务完成率 82%，非技术 54%       │
│  超过 3 天的待办最终完成率只有 20%     │
│  💡 建议: 重新评估陈旧待办...         │
│  ─────────────────────────────────  │
│  继续追问:                           │
│  ┌─────────────────────────┐ [发送]  │
│  │为什么非技术任务完成率低？  │         │
│  └─────────────────────────┘         │
└─────────────────────────────────────┘
```

### 对话管理

- 多轮对话，前端维护消息历史
- 每次追问把历史一起发给 AI
- temperature 0.7

### 从便签右键入口

选中某个便签 → 右键 → "问 AI 教练" → 弹出教练窗口，预填该便签上下文，提供快捷分析按钮（如何推进/拆分建议/时间估算）。

---

## 六、用户画像与记忆系统

### 问题

AI 教练需要了解用户是谁、目标是什么、当前阶段是什么，才能给针对性建议。
但信息不能无限堆砌——信息密度会极大影响上下文效果。

### 解法：Markdown 持久化记忆

```
~/.stickynotes/memory/
├── identity.md       ← 用户是谁（低频更新）
├── goals.md          ← 当前目标（中频更新）
├── patterns.md       ← AI 发现的行为模式（高频更新）
├── insights.md       ← 已给建议 + 采纳情况（去重精炼）
└── changelog.md      ← 记忆变更日志
```

### 各文件内容

**identity.md（相对稳定）:**
```markdown
- 角色: 前端开发，3 年经验
- 团队: 5 人小团队，负责前端架构
- 工作节奏: 朝九晚五
- 工具习惯: 重度快捷键用户
```

**goals.md（会变）:**
```markdown
## 核心目标
- 完成 Q2 产品方案（截止 5.15）

## 学习目标
- 学习 Rust，计划转全栈

## 改善目标
- 克服拖延症
```

**patterns.md（AI 主动发现）:**
```markdown
## 时间模式
- 上午 9-11 点效率最高
- 下午 2-4 点待办完成率最低（35%）

## 待办模式
- 技术任务完成率 82%，非技术 54%
- 超过 3 天未完成的待办最终完成率 20%

## 内容模式
- 便签平均 28 字，偏短
- 带具体数字的便签完成率更高
```

**insights.md（避免重复）:**
```markdown
## 已给建议
- 建议重要任务安排在上午 9-11 点
- 建议超过 3 天的待办重新评估

## 采纳情况
- ✅ 开始用蓝色标记技术任务
- ❌ 上午时间块建议（用户说有晨会）
```

### 更新机制

不是追加，是合并。每次 AI 对话后：

1. AI 提取本次对话的关键信息
2. 和现有记忆合并（矛盾的替换，重复的跳过）
3. 总长度控制在 500 tokens（约 375 中文字）
4. 超限时让 AI 自动压缩

### 记忆注入 Prompt

```rust
let system = format!(
    "你是用户的 AI 教练。\n\n\
     ## 你对用户的了解:\n{}\n\n\
     ## 你的任务:\n\
     基于用户画像和便签数据，给出针对性建议。\n\
     不要重复已给过的建议。\n\
     发现新行为模式时用 [MEMORY_UPDATE] 标记。",
    memory.to_prompt_context(),
);
```

### 记忆生命周期

```
新建 → 活跃使用 → 可能过时 → 被更新或删除

例: goals.md "完成 Q2 方案"
  → 5.15 完成后，便签里没有 Q2 任务了
  → AI 自动标记为已完成
  → 新目标出现时替换
```

### 初始化

首次启用 AI 时的引导问卷：

```
┌─────────────────────────────────────┐
│  🧠 AI 教练 — 先了解一下你            │
├─────────────────────────────────────┤
│  信息仅存在本地，不会发送到云端。      │
│                                     │
│  你的角色: [前端开发             ]    │
│  当前目标: [完成 Q2 产品方案      ]   │
│  想改善:   ☑ 拖延症  ☑ 便签太乱      │
│  工作节奏: (●) 朝九晚五              │
│                                     │
│  [跳过]              [保存并开始]      │
└─────────────────────────────────────┘
```

---

## 七、设置页

### 入口

- 托盘菜单 → 设置
- 快捷键: Alt+,

### 结构

```
设置窗口
├── [AI 设置] tab
│   ├── 默认模型: [Claude Sonnet 4 ▼]
│   ├── Claude API Key: [____________] [测试连接]
│   ├── OpenAI API Key: [____________] [测试连接]
│   ├── OpenAI Base URL: [____________]
│   ├── 隐私说明
│   └── 用户画像编辑区
├── [通用] tab
│   └── 语言: [自动 / 中文 / English]
└── [关于] tab
```

### 测试连接

```rust
async fn ai_test_connection(provider, api_key, base_url) -> Result<String, String> {
    // 发最简单的请求验证 key
    // 返回 "✅ 已连接 — model-name" 或 "❌ API Key 无效"
}
```

### 首次使用引导

用户第一次点 AI 功能但没配 key 时：

```
┌─────────────────────────────────────┐
│  🔑 需要配置 AI 服务                  │
├─────────────────────────────────────┤
│  AI 日报需要调用 AI 模型来生成。       │
│  请先在设置中配置 API Key。           │
│  支持 Claude 和 OpenAI。             │
│                                     │
│  [打开设置]  [取消]                  │
└─────────────────────────────────────┘
```

---

## 八、其他设计要点

### 成本估算

| 功能 | 输入 tokens | 输出 tokens | 单次成本 (Claude Sonnet) |
|------|------------|------------|------------------------|
| AI 润色 | ~200 | ~300 | ~$0.005 |
| AI 日报 | ~1500 | ~500 | ~$0.012 |
| AI 周报 | ~5000 | ~800 | ~$0.027 |
| AI 教练 (单轮) | ~3000 | ~600 | ~$0.018 |
| AI 教练 (5轮) | ~8000 | ~2000 | ~$0.054 |

每天用一次日报 + 偶尔润色，月成本约 $0.5-1。

生成前显示预估成本，让用户确认。

### 错误处理

| 错误 | 用户看到什么 |
|------|-------------|
| Key 无效 | "API Key 无效，请在设置中检查" + 跳转按钮 |
| 网络超时 | "网络连接超时，可重试" |
| 限流 | "请求太频繁，30 秒后重试" |
| 模型过载 | "AI 服务繁忙，稍后再试" |
| 内容被拒 | "内容无法处理，请修改后重试" |

### 隐私

- 设置页有隐私说明：便签内容会发送到 Anthropic/OpenAI
- 内容仅用于生成响应，不会被训练
- 可选：便签级 AI 排除开关（metadata 中标记 `ai_excluded: true`）

### Prompt 迭代

外部 prompt 文件，用户可编辑调优：

```
~/.stickynotes/prompts/
├── daily_report.md
├── weekly_report.md
├── coach_time.md
├── coach_completion.md
├── polish_casual.md
└── ...
```

首次运行自动创建默认文件，代码中有 fallback。

### 便签时间戳

Note 需要加 `created_at` / `updated_at`，在插件架构迁移时一起做：

```rust
pub created_at: String,   // ISO 8601
pub updated_at: String,   // ISO 8601
```

---

## 九、开发顺序

```
Phase 1: 插件架构迁移
├── 建立 plugin_system（Rust + JS）
├── 迁移 core / export / config 到插件
├── Note 加 created_at / updated_at / metadata
├── 所有测试通过，功能无变化

Phase 2: AI 基础设施
├── AiProvider trait + Claude 实现 + OpenAI 实现
├── AiService 统一入口
├── config.json 加 ai 配置块
├── 设置页（API Key 输入 + 测试连接）

Phase 3: AI 润色分享 ← 最简单，验证架构
├── 便签右键菜单加 "AI 润色"
├── 风格选择 + 流式预览 + 复制

Phase 4: AI 日报/周报
├── 托盘菜单加 "生成日报/周报"
├── 报告预览窗口

Phase 5: 用户画像 + 记忆系统
├── 初始化问卷
├── memory/ 目录 + 读写逻辑
├── 记忆注入 prompt

Phase 6: AI 教练
├── 独立窗口 + 对话式 UI
├── 四个分析维度
├── 从便签右键入口
├── 对话中自动更新记忆
```

---

## 十、涉及模块

```
M1 (app_core)   — 新增 ai/ 子模块（provider, service）
M2 (infra)       — SQLite 迁移（时间戳、metadata）
M3 (tauri-bridge)— 各插件的 commands
M4 (frontend)    — 各插件的 UI 组件
M5 (command)     — 迁移为插件注册模式
新增              — plugin_system/ (Rust + JS)
新增              — plugins/ai-share/, ai-report/, ai-coach/, settings/
新增              — ~/.stickynotes/memory/, ~/.stickynotes/prompts/
```

# V0.2.0 需求讨论

## 功能一：中英多语言自动识别

- 启动时自动检测操作系统语言
- 中文系统（zh-CN/zh-TW）→ 中文界面
- 其他语言系统 → 英文界面
- 无需手动切换，无需设置项

## 功能二：导出便签

### 导出方式

| 模式 | 行为 | 确认 |
|------|------|------|
| 复制式导出 | 标题+内容到剪贴板，便签保留 | 无需确认，直接执行 |
| 剪切式导出 | 标题+内容到剪贴板，便签清空 | 需要确认弹窗（防误触） |

### 导出细节

- 只导出标题 + 内容，不导出颜色、位置等元信息
- 导出目标：系统剪贴板
- 导出完成后提示"已复制到剪贴板"

### 记忆功能

- 记住用户上次勾选的便签ID列表
- 下次打开导出面板自动恢复选中状态
- 想改选择可以取消/新增

## 存储方案

| 存储位置 | 内容 |
|----------|------|
| SQLite (`~/.stickynotes/notes.db`) | 便签数据（标题、内容、颜色等） |
| JSON (`~/.stickynotes/config.json`) | 用户偏好（导出选择记忆等） |

## 涉及模块

```
M1 (app_core)  — 无改动
M2 (infra)     — 无改动（SQLite不变）
M3 (commands)  — 新增 export_cmd.rs（导出命令）
M4 (frontend)  — 导出UI + 多语言文本替换
M5 (command)   — 导出命令注册到菜单
新增           — src/lib/locale.js, src/lib/i18n.js
```
